# import smtplib
# from data import *

# my_email = EMAIL
# my_password = PASSWORD

# with smtplib.SMTP('smtp.gmail.com', port=587) as connection:
#     connection.starttls()  # securing connection
#     connection.login(user=my_email, password=my_password)  # Login
#     connection.sendmail(
#         from_addr=my_email,
#         to_addrs='bamnbang.id@gmail.com',
#         msg='Subject:Hell0\n\nThis is body of my email')

# import datetime as dt

# now = dt.datetime.now()
# year = now.year
# month = now.month
# day_of_week = now.weekday()
# print(day_of_week)

# date_of_birth = dt.datetime(year=1999, month=5, day=19)
# print(date_of_birth)
